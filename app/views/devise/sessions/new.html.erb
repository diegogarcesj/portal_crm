<h1 class="text-center">Bienvenido al Portal CRM</h1>
<h4 class="text-center">Aprendizaje 101</h4>

<h2>Iniciar sesión</h2>

<%= simple_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
  <div class="form-inputs">
    <%= f.input :email,
                required: false,
                autofocus: true,
                input_html: { autocomplete: "email" },
                label: 'Correo electrónico' %>
    <%= f.input :password,
                required: false,
                input_html: { autocomplete: "current-password" },
                label: 'Contraseña' %>
    <%= f.input :remember_me, label: 'Recordarme', as: :boolean if devise_mapping.rememberable? %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, "Iniciar sesión", class: "btn btn-primary" %>
  </div>
<% end %>

<%= render "devise/shared/links" %>
